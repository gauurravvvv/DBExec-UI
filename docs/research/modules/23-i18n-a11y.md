# 23 · i18n & Accessibility

DBExec has 10 locales. To complete:

## i18n gaps

- **RTL** support (Arabic, Hebrew) — flip layout via `dir="rtl"` +
  CSS logical properties.
- **Locale-aware formatting** — `Intl.NumberFormat`, `Intl.DateTimeFormat`
  on every numeric / date display.
- **Translation export/import** — CSV / XLIFF for translator handoff.
- **Pluralisation** — ICU MessageFormat. ngx-translate supports it
  via the messageformat compiler.
- **Locale picker on login** — done.
- **Locale persistence in JWT** — done.

## a11y gaps

| WCAG criterion | Status | Action |
|---|---|---|
| 1.1.1 Non-text content | 🟡 some images missing alt | Audit |
| 1.3.1 Info & relationships | ✅ semantic HTML | — |
| 1.4.3 Contrast minimum | 🟡 dark theme contrast | Audit |
| 1.4.10 Reflow | ✅ responsive | — |
| 2.1.1 Keyboard | 🟡 some custom controls trap or skip | Sweep |
| 2.1.2 No keyboard trap | 🟡 modals | Trap on open, restore on close |
| 2.4.3 Focus order | 🟡 sidebar peek | Test |
| 2.4.7 Focus visible | ✅ | — |
| 3.3.1 Error identification | ✅ Zod errors | — |
| 4.1.2 Name role value | 🟡 ARIA on charts | echarts `aria.enabled = true` |
| 4.1.3 Status messages | 🟡 toasts | `role="status"` on toast container |

## axe-core in tests

```ts
// e2e/utils/a11y.ts
import { Page } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

export async function a11yScan(page: Page, where: string) {
  const r = await new AxeBuilder({ page }).analyze();
  if (r.violations.length) {
    console.error(`a11y on ${where}:`, r.violations);
    throw new Error(`${r.violations.length} a11y violations on ${where}`);
  }
}
```

## Tests

- **A11Y-PAGE-H-01** — every list page passes axe with 0 critical
- **I18N-RTL-H-01** — Arabic locale flips layout direction
- **I18N-FMT-H-01** — number formatted as 1,234.56 in en-US and 1.234,56 in de
