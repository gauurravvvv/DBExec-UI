# db-access
> Update the Progress log on every change.
> Code path: `src/app/modules/db-access` · Status: 🟡 · Last updated: 2026-08-19

## 1. Context
- Responsibility: A UI over the **customer datasource's native PostgreSQL security model** — DB roles/users, GRANT/REVOKE privileges (table/column/schema/sequence/function), role memberships, default privileges, effective-privilege inspection, and live session management. NOT DBExec's own RBAC (that's the `role`/`users`/`groups` modules).
- Key files:
  - `roles/` — `list-db-roles` (roles + login-users, split FE-side on `canLogin`), add/edit/view db-role (attributes, memberships, owned objects, access-profile export).
  - `privileges/privileges-access` — the grant composer (compose grants + view effective privileges). `privileges/sessions` — live `pg_stat_activity` sessions with cancel/terminate.
  - `services/db-access.service.ts` — all HTTP (signal state); `services/describe-change.ts` — turns a mutation "intent" into a plain-language confirmation line (**no SQL is ever built or shown on the FE**); `services/access-export.util.ts` — client-side JSON/CSV export of an access profile.
  - `shared/datasource-picker/` — every screen is scoped to a chosen datasource. `components/change-summary-dialog` — preview + confirm for mutations.
- Depends on: **datasource** (the `:datasourceId` every route is scoped to; introspects that DB's catalog). `DB_ACCESS` api constants.
- Depended on by: — standalone admin tooling; loosely related to query-runner connections (both act on a datasource's native login).
- How it works: Two lazy modules mounted separately in app-routing — `db-roles` (path `db-roles`) and `db-privileges` (path `db-privileges`, with `sessions` child), both sharing `DbAccessSharedModule` + the `dbPrivileges` permission. Pick a datasource → service calls `/db-access/:datasourceId/...` → BE opens the target Postgres and queries its catalog live. Every mutating endpoint accepts `previewOnly:true` → returns `{ masked: string[] }` (a SQL preview the user confirms) instead of executing; destructive ops require `confirm:true`. `capability` endpoint tells the UI what the engine supports.
- Decisions: **FULLY STATELESS / live-only** — the target datasource's Postgres catalog is the ONLY source of truth; nothing is mirrored or persisted to DBExec's DB. FE never constructs or displays raw SQL — it POSTs a structured change-set intent, BE emits the SQL. Lists use `app-custom-table` (`list-db-roles` was the original pilot for the custom-table rollout). See [ARCHITECTURE.md](../ARCHITECTURE.md).
- Gotchas / constraints: Postgres-centric (the native-security model + `pg_stat_activity` sessions assume PG; capability-gated for other engines). `us-data-grid` retired here but `UsGridCellDirective` kept (custom-table depends on it). Sessions migrated to app-custom-table (1e482a19). The BE audit-log-system note flags db-access **grants** as an audit-coverage gap (connection CRUD + grant/execute events not fully diffed).

## 2. Goals
- Objective: Safely inspect and manage a datasource's native DB security (roles, grants, memberships, sessions) with preview-before-execute and no local state drift.
- Current focus: — none active.
- Next up: — (potential: broaden beyond Postgres; close the audit-coverage gap on grants).
- Out of scope: DBExec app RBAC (role/users/groups), RLS row filters (rls-rules).

## 3. Progress (newest first)
### 2026-08-19 — hub cleanup: card removed, two features deleted, sessions/templates/advanced UX
- **Removed who-can-access + PUBLIC Grants end to end** (user couldn't see a use case). FE: deleted `who-can-access/` + `public-hardening/` folders, dropped the two hub tabs + ngSwitch bodies + module routes/declarations, removed `loadWhoCanAccess`/`loadPublicGrants` service methods + `WHO_CAN_ACCESS_SUFFIX`/`PUBLIC_GRANTS_SUFFIX` api constants + the two standalone-header buttons/nav methods in privileges-access. BE: deleted `whoCanAccess.ts` + `listPublicGrants.ts` controllers, their routes, `getWhoCanAccess`/`getPublicGrants` + `WhoCanAccessRow`/`PublicGrantObject` from pgIntrospect. Removed 7 orphan i18n keys (WHO_CAN_ACCESS_*, PUBLIC_HARDENING*) from all 10 locales. Verified live: both routes now 404. Hub is now 4 tabs: Compose / Effective / Sessions / Templates.
- **Card model (FINAL): ONE parent hub card, flush bodies, no inner cards.** (User first asked to drop the outer card, then reversed: keep the parent card, remove the inner ones.) `.settings-content-container` carries the single card surface (--card-background / --radius-md / --shadow-sm). Each embedded child (privileges-access, templates list, **sessions**) painted its OWN card via the db-access-page/list mixins with higher specificity than the hub's `::ng-deep` flatten, so each got a `[class.embedded]` root hook + a scoped rule that zeroes background/radius/shadow/padding when embedded (reliable, no specificity fight). Net: one card, tab bodies flush inside, never card-in-card.
- **Effective "Pick a role" width** capped to match Compose's "Select role": `.eff-role-picker { width:100%; max-width:720px }` (same 720px cap as `.guided-panel`), so it no longer stretches the full left column. The tree below stays full-width.
- **Effective tab: vertical divider** between Effective (left) and Default privileges (right). The two `.rail-card`s lost their individual card treatment; a single `border-left` on the right column is the divider (becomes a horizontal `border-top` when stacked < 1100px).
- **Role Templates: Add button moved to the hub header** (top-right, parity with Compose's Apply). Hub got `@ViewChild(ListDbTemplatesComponent)` + `addTemplate()` → `templatesRef.onAdd()`; the list hides its own header/Add row when embedded.
- **Fixed a pre-existing i18n bug**: the templates Type column showed raw `DB_ACCESS.BUILT_IN`/`DB_ACCESS.CUSTOM` — those keys lived under THEME_SETTINGS, not DB_ACCESS. Added properly localized `DB_ACCESS.BUILT_IN`/`CUSTOM` to all 10 locales.
- **Active Sessions toolbar** consolidated to one aligned row (search + content-sized status dropdown + hide-background + export/refresh); stray floating Clear folded in-row (shows only when a filter is active). (agent)
- **New Template form** rebuilt to the canonical Add-Role/Add-User skeleton (db-access-form mixin, 50% form, shared app-custom-* controls, back button, rules single-column). (agent)
- **Advanced (SQL rules) compose**: 2-up field grid (Action+Level / Schema+Grantee) instead of a tall single column; privileges wrap compactly; incomplete marker recoloured from alarming amber → calm neutral (`--secondary-color` edge + neutral chip); form CLEARS on Guided↔Advanced switch (blankRule() reset + guided state cleared); Apply already gated on allRulesValid.
- Gates green both repos (tsc/ngc/prod build); i18n parity verified (326 DB_ACCESS keys × 10 locales).

### 2026-08-19 — all db-access lists/dropdowns sort ASC at the SQL level
- Decision (user): every list + dropdown in the module defaults to ascending, and the sort must live at the **SQL level** (ORDER BY in the introspection query) — not BE JS, not FE JS.
- Audit result: already SQL-ASC at source — schemas (`ORDER BY n.nspname`), tables (`table_name,…`), roles/grantees (`r.rolname`, default sortDir asc), effective tree (schema>table>priv), public grants (`schema, table`; grantee is always PUBLIC so "by grantee" is moot). `who-can-access` groups a single-object result set into cards and already sorts privileges A→Z + grantees A→Z with PUBLIC pinned last (legit client transform, one object, not paged).
- The ONE surface that wasn't SQL-sortable: **Default privileges**. BE `listDefaultPrivileges` returned a raw `acl` STRING per (owner,schema,type); FE exploded it into grantee rows in JS, so the sort couldn't be in SQL. Rewrote the query to `CROSS JOIN LATERAL aclexplode(d.defaclacl)` + `array_agg(privilege_type ORDER BY privilege_type)` + `GROUP BY … ORDER BY schema NULLS FIRST, object_type, grantee`. BE now returns ready `{ownerRole, schema, objectType, grantee, privileges[], scope}` (additive — `acl` string dropped from the shape; exportGrants/controller just pass rows through). FE `loadDefaults()` dropped `parseDefaultAcl()` + `ACL_CHAR` + the JS `.sort()` entirely — renders BE rows directly.
- Verified end-to-end: seeded 3 defaults across analytics/sales + PUBLIC/read_only in OrgOneMasterDB, hit real `GET /db-access/:ds/default-privileges` (200) → rows arrive analytics→sales, PUBLIC before read_only, INSERT before SELECT; browser panel renders same order; fixtures reverted (pg_default_acl back to 0). Gates green both repos.
- Files: dbexec-api pgIntrospect.ts (`DefaultPrivRow` shape + query), privileges-access.component.ts (loadDefaults simplified).

### 2026-08-19 — effective-tab tree cut fixed + Default-privileges panel display bug fixed
- **Effective tab tree was clipped** (in the hub's Effective tab / composer rail): the `<app-privilege-tree>` there had no fill mode, so it collapsed to its ~120px intrinsic height and was clipped by `.rail-card { overflow:hidden }`. Fix: `[fill]="true"` on the effective-tab tree + made `.rail-card` a flex column with `.eff-tree-embed { flex:1;min-height:0 }` so the tree fills the card and scrolls internally. (Same fill pattern as view-db-role.)
- **Default privileges panel showed blank/empty even when defaults exist:** the template bound `d.privileges` + `d.grantee`, but `GET /default-privileges` returns `{ownerRole, schema, objectType, acl}` where `acl` is a raw aclitem[] string (`{read_only=r/postgres}`). Added `parseDefaultAcl()` + `ACL_CHAR` map in the component to explode the acl into `{scope, grantee, privileges[]}` display rows; template binds those. Verified end-to-end (created a real ALTER DEFAULT PRIVILEGES, panel rendered "sales · TABLES / SELECT / read_only", then reverted). NOTE: an EMPTY panel is legitimate when pg_default_acl is empty — not a bug by itself.
- Verified: tsc+ngc+prod build clean; live on the hub effective tab.
- Files: privileges-access/privileges-access.component.{ts,html}, styles/_db-access-shared.scss.

### 2026-08-19 — tree search fixed (table + privilege), search is now the shared component
- **SearchInputComponent made STANDALONE** (was declared in SharedModule): removed from SharedModule `declarations`, added to `imports`, still `exports`ed → all ~40 module consumers unaffected, AND standalone components can import it. `app-lazy-tree` now imports it and uses `<app-search-input>` as its BUILT-IN search (replaced the raw `<input>`), so every tree consumer gets the shared debounced search+clear. (Projected `#search` slot kept as an override path but privilege-tree no longer needs it.)
- **Search now matches TABLE names + PRIVILEGE names, not just schema:** BE `getEffectivePrivTree` schema-mode search widened to `sch ILIKE OR tbl ILIKE OR priv ILIKE` (so searching a table/priv surfaces its parent schema); table-mode widened to `tbl ILIKE OR priv ILIKE`. FE: `app-lazy-tree.loadChildren` now carries the active search into BRANCH child loads (leaves load unfiltered), and privilege-tree forwards `ctx.search` to the level-1 (tables) load. Verified live: search "orders" → sales; "insert" → sales; drilling stays filtered.
- Verified: BE tsc + FE tsc/ngc/prod build clean.
- Files: shared/components/search-input/search-input.component.ts (standalone), shared/shared.module.ts, shared/components/lazy-tree/* , components/privilege-tree/* , (BE) services/pgIntrospect.ts.

### 2026-08-19 — view-db-role tree polish: single scroll, shared search, Inter font, one-row toolbar
- **Tree height modes (final):** the clamp(vh) default gave INCONSISTENT results across screens (short → card double-scrolls; tall → empty gap). Replaced with a `[fill]` mode on app-lazy-tree: the tree FLEXES to fill its bounded flex-column parent, so the tree body always takes exactly the remaining height (same behavior every screen; tree scrolls internally, card doesn't; graceful `.view-container` fallback scroll only on very short viewports). Flex chain: `.view-container`→`.effective-card`(flex:1,min-h:280)→`app-privilege-tree`(:host.pt-fill flex:1)→`.priv-tree`→`app-lazy-tree`(:host.lt-fill flex:1)→`.lazy-tree`→`.lt-root.lt-scroll`(flex:1,min-h:120,overflow auto). view-db-role passes `[fill]="true"`. app-lazy-tree still supports `[maxHeight]` (px/clamp override) + `'none'` (ancestor scrolls) for non-fill consumers (composer clamp default stays). Verified across viewport heights: tall → containerScrolls:false + tree fills; short → tree at floor + container fallback.
- **Shared search:** the generic tree gained a `#search` projection slot (`@ContentChild('search')`) receiving `applySearch` as `$implicit`; privilege-tree projects `<app-search-input>` (already debounced) instead of the tree's built-in input. Built-in remains the fallback when no `#search` is projected.
- **Font:** dropped `mono:true` from privilege-tree's `levels` → labels render in Inter (was JetBrains Mono, inconsistent with the page).
- **Toolbar one row:** `.lt-toolbar` is `flex-wrap:nowrap`; projected search flexes (max 360px), host toolbar slot (system toggle, `margin-left:auto`, nowrap) pinned right.
- Verified: tsc+ngc+prod build clean; live on MasterUser.
- Files: shared/components/lazy-tree/* , components/privilege-tree/* , roles/view-db-role/view-db-role.component.html.

### 2026-08-19 — Intent-first compose, reflect-current-state, 2 new screens, generic lazy-tree (FE)
- **Generic `app-lazy-tree`** (`shared/components/lazy-tree/`, standalone): data-agnostic recursive tree — `[loadNodes]` fn, `[params]`, `[levels]`, `[maxHeight]` (scroll), `[searchable]`, `[lazyTreeToolbar]` + `#nodeBadge` + `#leaf` projection, expandable leaves. `privilege-tree` refactored to a thin wrapper over it. Registered in db-access-shared.module. Reuse app-wide.
- **Intent-first Compose (Slice 2):** privileges-access compose tab now defaults to a GUIDED flow (WHO→WHAT→WHERE) with presets (Read / Read+write / Custom) + a Guided|Advanced(SQL rules) toggle. Drives rules[0], feeds the EXISTING applyChanges→previewOnly→change-summary-dialog→execute pipeline. Verified live: guided grant to analyst_dbexec on sales.* executed + confirmed in DB.
- **Reflect-current-state / diff-apply (user request):** picking grantee+schema in guided mode loads the role's current DIRECT grants (loadRoleGrants) and PRE-SELECTS held privileges with a "HAS NOW" tag + hint ("untick to revoke, add to grant"). currentPrivs Set; reflectCurrentState() keyed by grantee::schema. So you edit against reality.
- **Role-form SQL gate (Slice 3):** add/edit-db-role Save now routes through previewOnly → change-summary-dialog (typed-phrase for SUPERUSER/BYPASSRLS). Fixes the one-click-superuser bug. No new i18n.
- **PUBLIC hardening screen (Slice 4/FEAT-D4):** `privileges/public-hardening/` — app-custom-table, TRUE server paging (BE fixed, see API log), multi-select revoke → change-set revokePublic → SQL preview. Verified live (shows the 2 PUBLIC objects in fixture).
- **Who-can-access (FEAT-D9):** `privileges/who-can-access/` — datasource→schema→table server-lazy pickers → per-role cards with privilege chips + direct/via-PUBLIC provenance. Verified live on sales.orders (3 roles).
- Nav: composer header gained "Who Can Access" + "PUBLIC Grants" buttons (+ existing Templates/Sessions). Routes added in db-privileges.module.
- i18n: +14 (guided) +23 (public/who) +3 (reflect) = 40 new DB_ACCESS keys × 10 locales, full parity.
- Built partly via 3 parallel sub-agents (public-hardening, role-form gate, who-can-access); MAIN owned generic tree + intent-compose + reflect-state + all shared-file merges (module/i18n/nav) + BE paging fix + integration.
- Verified: tsc + ngc + `ng build --configuration production` all clean; live end-to-end against TestDB→OrgOneMasterDB.
- Files: shared/components/lazy-tree/* (new), components/privilege-tree/*, privileges/{privileges-access,public-hardening,who-can-access}/*, roles/{add,edit}-db-role/*, privileges/db-privileges.module.ts, db-access-shared.module.ts, services/db-access.service.ts, core/constants/api.constant.ts, assets/i18n/*.

### 2026-08-19 — Effective-privilege tree rebuilt + unified (Slice 1, FE)
- **Unified the two effective-priv viewers:** `PrivilegeTreeComponent` MOVED from `db-roles.module` into `db-access-shared.module` (declared + exported) so BOTH the role-detail page and the composer use ONE tree. The composer's Effective tab client-side `cdk-virtual-scroll` flat list (the 191-row scroll the user flagged) is REPLACED by `<app-privilege-tree>`. Removed the dead `groupEffective`/`filteredGroups`/`effectiveFilter`/`EffectiveGroup` machinery from `privileges-access`.
- **Tree upgraded** (server-lazy, nothing client-side): summary line ("<role> can act on N objects — X direct, Y inherited, mostly via <src>"), **Direct | Inherited | All** provenance segmented control (server `?provenance=`), **Show system schemas (N)** toggle (server `?includeSystem=`), search across schema/table/privilege. New service methods `loadEffectiveSummary` + `loadEffectiveTree` gained `provenance`/`includeSystem`. API const `EFFECTIVE_SUMMARY_SUFFIX`.
- **Styling bug fixed (user-reported invisible labels):** component used NON-EXISTENT tokens (`--text-color-secondary`, `--surface-hover/100/200`) → transparent text on the org's dark theme. Swapped to real tokens: `--text-muted`, `--secondary-color`, `--hover-background`, `--secondary-background`, `--card-background`. NOTE: there is no base dark scss — dark look is the org's runtime theme rewriting tokens, so only real token names render. Also fixed pre-existing broken hover/muted refs in the same file. Privilege chips now render as bordered pills (privilege+provenance grouped) instead of running together.
- **The fix that made it actually work:** the BE `.passthrough()` param-validator fix (see BE log) — before it, the tree only ever showed PUBLIC grants.
- i18n: +12 keys (EFF_*, SEARCH_TREE_PLACEHOLDER, SHOW_SYSTEM_SCHEMAS) × 10 locales.
- Verified: `tsc` + `ngc --noEmit` + `ng build --configuration production` all clean; live end-to-end (read_write → 4 objects / 2 direct / 2 inherited, orders leaf = INSERT/SELECT/UPDATE direct).
- Files: components/privilege-tree/* , db-access-shared.module.ts, roles/db-roles.module.ts, privileges/privileges-access/* , services/db-access.service.ts, core/constants/api.constant.ts, shared/validators/db-access.ts, assets/i18n/*.

### 2026-08-19 — UX enrichment audit + backlog + mockups (DESIGN ONLY, no code)
- User finds both screens confusing; asked for a bugs/features/polish README + HTML mockups; full-stack build scope. Audited code + live app.
- FE-specific findings: **two effective-priv viewers** — the good lazy `components/privilege-tree` (used in view-db-role) vs a client-side grouped `cdk-virtual-scroll` flat list in `privileges-access` Effective tab (this is the 191-row flat scroll the user flagged). **Fix: reuse the tree everywhere** (move PrivilegeTreeComponent from db-roles.module into db-access-shared). Composer's compose-rule is SQL-grammar-first (Action→Level→Schema→Grantee); **redesign to intent-first (WHO→WHAT→WHERE→diff preview)**. Add/Edit role forms have **NO Review-SQL gate** despite SUPERUSER/BYPASSRLS. Datasource scope is memory-only (refresh silently auto-switches DB). `flagsOf` (list) vs `flags()` (view) disagree. 3-4 different privilege pickers; 3 confirm-dialog patterns; page-shell drift (templates list). Dead code: `liveSummaries` computes a pending-changes preview that's never rendered.
- Backlog + mockup live in the BE repo: `dbexec-api/docs/superpowers/specs/2026-08-19-db-access-ux-enrichment-backlog.md` + `.../2026-08-19-db-access-mockups/db-access-redesign.html`. Artifact: https://claude.ai/code/artifact/bf9b50bd-8acb-4f6d-9b3a-7082c6a723d2
- Three cross-cutting rules locked: read-only BE-built SQL preview popup on every action; every table = `app-custom-table` infinite-scroll (no paginators); everything server-lazy (no FE search/sort/filter — replace composer client-grouping + tree client-search).
- **RLS dropped from db-access (user decision):** no RLS view/config on these screens — belongs to the `rls-rules` module only. FEAT-D6 removed from backlog + mockup (nav + screen gone; mockup now 7 screens).
- No FE code changed this session. Next: plan → build per roadmap.
- Files touched: docs/context/modules/db-access.md (this repo); backlog + mockup in dbexec-api.

### 2026-08-10 — PDM code-review fixes (FE)
- High-effort workflow review → FE fixes:
  - **Membership revoke was fully broken (critical):** membershipCall sent `toRole` to /memberships/remove, but the BE requires `fromRole` → every revoke 400'd. Fixed to send `fromRole: principal`.
  - **Membership grant+revoke atomicity:** the two calls now run SEQUENTIALLY (grants then revokes) and STOP at the first failure on execute; a partial apply surfaces the `MEMBERSHIP_PARTIAL` message instead of a bare failure toast (true single-txn would need a combined BE endpoint — out of scope).
  - **Restored WITH ADMIN OPTION:** re-added the `membershipAdminOption` toggle (shown when the diff grants ≥1 role) — the rebuilt dialog had dropped it.
  - **Effective inspector column grants:** groupEffective now separates table-wide vs column-only privileges; a column-only privilege renders as "PRIV (col)" so it isn't misread as whole-table. privCount counts the rendered set.
  - **applyTemplate no longer clobbers form state:** only patches attribute flags the template explicitly sets (user's other toggles preserved).
  - **goToTemplates uses DB_ACCESS.TEMPLATES_LIST constant** (was a hard-coded path).
- Note (accepted, not a bug): global "Manage Membership" header button remains removed — membership is per-principal via the row action by design; a principal not on the current page is reachable via search. Revisit if users need a picker-first entry point.
- Verified: tsc + ngc + prod build clean. +1 i18n key (MEMBERSHIP_PARTIAL) × 10 locales.
- Files touched: roles/list-db-roles/{list-db-roles.component.ts,.html}, roles/add-db-role/add-db-role.component.ts, privileges/privileges-access/privileges-access.component.ts, assets/i18n/*.json

### 2026-08-10 — PDM Phase 2 (FE): templates screen + apply, clone-privileges, composer nav
- Done:
  - **Templates manage screen (PDM D10):** new `services/db-template.service.ts` (signal CRUD over /db-access/templates) + `privileges/templates/list-db-templates` (server-paged app-custom-table; scope + built-in badges; built-ins read-only — Edit/Delete disabled; delete via app-justification-dialog with requireJustification=false) + `privileges/templates/edit-db-template` (create/edit in one; name/description/scope [org-wide vs datasource picker] + an abstract privilege-rule builder repeater; canSave gating). Routes added under db-privileges module (`templates`, `templates/new`, `templates/:id/edit`) — NO new sidebar entry (hosted under Privileges). Mirrored validator `shared/validators/db-role-template.ts`.
  - **Apply template in add-db-role:** template picker (org-wide + pinned) prefills attribute toggles from the recipe; hint shows rule count (privileges composed later on the Privileges screen).
  - **Clone privileges (PDM D1):** clone mode gains a "Copy privileges" toggle; on create success it fetches the source role's direct grants (loadRoleGrants) and applies them to the new role as one change-set.
  - **Composer nav:** privileges header gains a "Templates" button (→ manage screen) beside "Active Sessions".
  - API constants + routes constants for templates/reassign/public-grants/database-privileges/rls/who-can-access. i18n: +23 DB_ACCESS keys across all 10 locales (templates screens, apply picker, copy-privileges, rule builder).
- Verified: `tsc --noEmit` clean; `ngc -p tsconfig.app.json --noEmit` clean; `ng build --configuration production` clean (db-privileges chunk 89→128 kB with templates).
- Deferred (BE endpoints exist, FE surfaces not yet built): bulk grant/revoke multi-select in the roles list; the read PANELS for PUBLIC hardening / database-schema privileges / RLS view / reverse-lookup inside the composer; password/expiry lifecycle fields; session policy hints.
- Files touched: services/db-template.service.ts (new), privileges/templates/** (new list + edit), privileges/db-privileges.module.ts, privileges/privileges-access/* (nav), roles/add-db-role/* (apply + clone), core/constants/{api,routes}.constant.ts, shared/validators/db-role-template.ts, assets/i18n/*.json

### 2026-08-10 — PDM Phase 1 (FE): SQL-review dialog, lazy privilege tree, diff-apply memberships
- Done:
  - **Fixed the reported `Object/Type = —` bug** on the role/user detail page: replaced the flat `p-table` (bound the non-existent `row.object`/`row.objectType`) with a new `components/privilege-tree/` — a lazy-expanding schema → table → privilege-chips tree with `direct`/`via <role>` provenance + a WITH-GRANT-OPTION marker + a server-search box. Every level is server-driven (`GET …/effective/:role/tree?level=…`), so it scales to large catalogs. Removed the now-dead `effective`/`loadEffective` from `view-db-role`.
  - **Review-SQL dialog:** upgraded `change-summary-dialog` to render the exact (masked) SQL the BE now returns per statement, each with a DANGER badge + reason, plus a collapsible SQL panel (open by default). Confirm gating: neutral → 1 click; destructive → "I understand"; critical → must type the exact `confirmPhrase` (BE re-checks). Dialog now emits the typed phrase on confirm.
  - Wired the widened preview through `list-db-roles` (`runPreviewAndArm`/`confirmPreview($event)`) and `privileges-access` (`openConfirm`/`confirmApply($event)`): both bind `statements`, `confirmPhrase`, and thread the typed phrase into the execute body.
  - **Membership → "Manage roles for X" (diff apply, B1):** the per-row action now opens a dialog pre-checked with the principal's CURRENT member-of set; checking = grant, unchecking = revoke; Apply computes the diff (`toGrant`/`toRevoke`), previews grants+revokes as one merged change-set, then executes. Removed the old attach/detach mode toggle + the global toolbar membership button.
  - Add-role: `cloneFrom` is required in clone mode; Save gated via new `canSave` getter (datasource + form valid + not saving + clone source when cloning). Mandatory `*` already present via `app-custom-*` `[required]`.
  - Service: `loadEffectiveTree` + `loadRoleGrants`; API constants `EFFECTIVE_TREE_SUFFIX`, `ROLE_GRANTS_SUFFIX`. i18n (all 10 locales): `DB_ACCESS.SEARCH_SCHEMAS_PLACEHOLDER/GRANTABLE/REVIEW_SQL(reworded)/DANGER_BADGE/MANAGE_ROLES/MANAGE_ROLES_FOR/MANAGE_ROLES_HINT/MEMBERSHIP_DIFF` + top-level `PDM.DANGER.*` (BE-emitted reason keys).
- Verified: `tsc --noEmit` clean; `ngc -p tsconfig.app.json --noEmit` clean; `ng build --configuration production` clean (db-roles + db-privileges chunks build).
- Next (Phase 2): templates manage screen + apply picker; clone-privileges "copy grants"; bulk grant/revoke; new composer panels (database/schema privs, PUBLIC, RLS view, reverse lookup); password/expiry lifecycle; session hints.
- Files touched: components/privilege-tree/* (new), components/change-summary-dialog/*, roles/{list,add,view}-db-role/*, roles/db-roles.module.ts, privileges/privileges-access/*, services/db-access.service.ts, core/constants/api.constant.ts, assets/i18n/*.json

### 2026-07-24 — Current state captured
- Done: Stateless live PG security console is complete — roles/users list, add/edit/view role (attributes/memberships/owned/export), grant composer with effective-privilege view, default privileges, preview-only + confirm on every mutation, plain-language change descriptions (no FE SQL), access-profile JSON/CSV export, live sessions with cancel/terminate. Migrated to `app-custom-table` (roles pilot 0baee618 + sessions 1e482a19); admin modules → `app-chip` (481422e7); reference-data DB-driven dropdowns (3d27b282); density switch dropped + scroll chain fixes.
- In progress / Known issues: Postgres-only for now (capability-gated). Audit coverage of grant/connection events is a flagged gap (audit-log-system).
- Next: —
- Files touched: docs/context/modules/db-access.md

### 2026-07-24 — Initialized from repo audit
- Done: Context + Goals seeded from the actual code (module folder `src/app/modules/db-access`).
- In progress: —
- Next: Enrich with specifics as work touches this module.
- Blockers: —
- Files touched: docs/context/modules/db-access.md
