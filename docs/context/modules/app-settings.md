# app-settings
> Update the Progress log on every change.
> Code path: `src/app/modules/app-settings` · Status: 🟢 · Last updated: 2026-08-07

## 1. Context
- **Responsibility:** The org's Settings surface, delivered as **two tabbed hubs** reached from the sidebar Settings group:
  - `/app/settings/app` → **App Settings** hub: Theme · Branding · Announcements (look-&-feel).
  - `/app/settings/system` → **System Settings** hub: SSO · Email · Security Policy · AI Features (org runtime config, mostly on the per-org `OrgPolicy` entity).
- **Key files:**
  - `components/app-settings-hub` / `system-settings-hub` — `p-tabView` containers. One "Save" button in the hub header delegates to the ACTIVE tab via the `SettingsTabForm` contract; active tab mirrored to `?tab=` for deep links; only the active tab is instantiated (`*ngIf`).
  - `settings-tab-form.ts` — the `SettingsTabForm` interface (`onSave()`, `dirty`, `busy`) every tab implements so the hub's single Save works.
  - Tab bodies: `theme-settings`, `branding-settings`, `sso-settings`, `email-configuration`, `security-policy`, `ai-features`; announcements is a nested CRUD (`list/add/edit/view-announcement`) with its own leaf routes.
  - `services/org-policy.service.ts` — signals (`current/loading/saving`); `getPolicy()` + `updateSecurity/updateEmail/updateSso`. `theme-settings.service`, `branding-settings.service`, `announcement.service` back their tabs.
  - `app-settings-routing.module.ts` — `'' → app`; the old per-screen routes are gone (they're tabs now); announcement CRUD keeps leaf routes.
- **Depends on:** BE `OrgPolicy` (`/org-policy`, `/org-policy/sso`), theme/branding/announcement endpoints, `HttpClientService`, `role.guard`, shared `app-custom-*`. **Depended on by:** `auth` login reads the SSO toggle; theme applied app-wide via `theme.service`; `ai-workspace` (Dex) reads the AI Features config; announcements surface in the shell.
- **How it works (permission gating — the crux):** Module HEADERS carry no `level`, only LEAVES do — so `canRead('appSettings')`/`canRead('systemSettings')` is ALWAYS false even for a full admin. Each hub route therefore gates on a **child leaf** the org admin holds: App hub on `THEME_MANAGEMENT`, System hub on `SSO_CONFIGURATION`. Holding the leaf shows the whole hub (sub-tabs are NOT individually gated). BE write routes gate on the matching leaf (e.g. `ssoConfiguration` WRITE for `PUT /org-policy/sso`).
- **Decisions:** Sidebar consolidated into one Settings group with two hub leaves (commit f93460a1, fe254971). Permission catalog split `appSettings` (theme/branding/announcements) vs new `systemSettings` (sso/email/security/ai) — see memory [[settings-sso-shipped]]. SSO cert + email password + AI API key are **write-only**: BE returns a `*Configured` boolean, never the value → UI shows a masked "Configured" state (empty string clears, omit keeps). AI Features = openai-compat provider + base URL + model + temperature; needs a FRONTIER model to actually drive Dex (see [[dex-full-app-control]]).
- **Gotchas:** Do NOT gate a hub route on the module header (`systemSettings`/`appSettings`) — the guard will always block ("can't open App Settings" bug); gate on a leaf. `POST /org-policy/backfill-settings` (gated `securityPolicy` WRITE) seeds systemSettings leaves for EXISTING orgs — the catalog seed is NOT re-run on boot; fresh orgs get it at onboarding, existing prod orgs need the backfill. Turning SSO off clears the IdP fields (load patches with `emitEvent:false` so it doesn't clear on load). SSO happy-path is not live-verified (needs a real IdP).

## 2. Goals
- **Objective:** One coherent, permission-correct Settings surface; secrets never round-trip; each tab saves through the shared hub Save.
- **Current focus:** — none active.
- **Next up:** Live-verify SSO save→reload (cert masked) + SSO login toggle end-to-end; verify backfill on an existing org.
- **Out of scope:** Platform-wide (cross-org) settings; per-user preferences (none — theme is org-level).

## 3. Progress (newest first)
### 2026-08-07 — Relay renders in the org theme (login sequence fix)
- The relay flashed the DEFAULT palette because (a) `stashLoginResponse` called `applyAuthArtefacts(null, null, locale)` → `applyFromLogin(null)` CLEARED any pre-painted theme, and (b) `login()` pre-painted via `void fetchAndApplyPublicTheme(org)` (fire-and-forget), so it navigated to `/auth/relay` before the theme landed.
- Fix (LoginService): `stashLoginResponse` now applies ONLY the locale (never nulls theme/branding); `login()` and `completeSamlLogin()` now **await** `fetchAndApplyPublicTheme(org)` before returning, so the login component navigates to `/relay` only after the org theme is painted. Net sequence: `POST /auth/login → GET /theme/public (awaited, paint) → navigate /relay → GET /auth/session (authoritative theme) → navigate home`. Every page after the user leaves /login is themed; no default-theme flash. The relay's own best-effort `fetchAndApplyPublicTheme` stays as a safety net for direct visits/refresh. `/theme/public` always 200s + all errors swallowed, so it never blocks login.
- Verified FE tsc 0, ngc 0, prod build. version_261.

### 2026-08-07 — Theme/Branding list fixes (icons, preview restore, sort, filter/export)
- **Duplicate icon**: Try-out and View both used `pi-eye`. Removed the row View button (the name link already navigates to view), switched Try-out to `pi-palette` (stop = `pi-times-circle`).
- **Stop-preview restore**: `stopTryOut()` re-applied `themeInjector.theme()`, but previewing had overwritten that signal with the *preview* theme → stopping re-applied the preview. Fixed by snapshotting the active theme (`activeThemeSnapshot`) at the FIRST try-out (kept across preview→preview switches), restored on stop / apply-supersede / ngOnDestroy.
- **i18n**: added `THEME_SETTINGS.PREVIEW` to all 10 locales (was missing → raw key showed).
- **Sort**: `listThemePresets` (BE) hardcoded `order:{sortOrder,createdOn}` and ignored the FE `sort` param, so clicking Name did nothing. Now parses `sort` (JSON `[{field,order}]`, same shape as /roles) with a column allowlist (`name`/`type`/`status`/`createdOn`), default seeded-first fallback. Same fix on `listBrandingPresets` (`name`/`status`/`createdOn`).
- **Filter + export**: both list tabs had them off. Enabled `showColumnFilters`+`enableExport` (parity with list-role/list-user) + set `title` for the export filename. Name column filter maps to BE `search`; export is client-side (loaded rows → CSV/XLS).
- Verified BE tsc 0, FE tsc 0, ngc 0, prod build. version_261.

### 2026-08-07 — Shared app-tabs component; unified tab theming
- Root cause of the "tab header not on theme" report: there was NO shared tab UI — a raw (unthemed) PrimeNG default plus 4 divergent hand-rolled `::ng-deep .p-tabview` overrides (App Settings, System Settings, view-dataset, view-alert), so the strip drifted and rendered at `--fs-control` (~11px) instead of a real tab size.
- Built **`app-tabs`** (`src/app/shared/components/tabs/`, standalone, OnPush): a themed header-only strip (pure buttons + active underline, `role=tablist`), fully token-driven so it follows the org theme incl. dark. API: `[tabs]` (`AppTab[]`: value/label(i18n)/icon/permission/count/disabled), `[(active)]`, `(activeChange)`, `size`, `fill`. Per-tab `permission` filters visibility via `PermissionService.canRead` (the HasPermission *directive* is non-standalone, so gating is done in TS). Caller renders the active body via `[ngSwitch]`.
- Converted BOTH hubs off `p-tabView`: App Settings (list bodies via ngSwitch) and System Settings (kept its card + shared Save + form bodies; only the strip swapped). Removed both hubs' local `::ng-deep .p-tabview` blocks. Added ONE canonical themed `.p-tabview` default in `styles.scss` so the 3 remaining `p-tabView` screens (view-dataset, view-alert, privileges) look consistent immediately; those keep their own local overrides where present (migrate to app-tabs later).
- Live-verified on :4200: App Settings + System Settings both render `app-tabs`, old `.p-tabview-nav` gone, active tab = primary colour + underline + weight 600, label `--fs-body` (12.25px, up from 11.375px), `aria-selected` correct; tab click swaps body + syncs `?tab=` + (App Settings) flips the common Add label. System Settings keeps its card surface + Save button. Gates: FE tsc 0, ngc 0, prod build 0 (2.69 MB). version_261, awaiting push. `app-tabs` added to the UI-kit line in ARCHITECTURE.md.

### 2026-08-07 — App Settings hub: list-role header/card parity
- All 3 tabs (Theme/Branding/Announcements) are now standard list screens using the shared `<app-custom-table>` + `UsServerListAdapter` (server paging, limit 50, `search` global key). Converted the Theme list from the earlier gallery to the same table shape as list-user/list-role; built the Branding preset library FE (list-branding + add/edit routed page) mirroring Theme; Announcements already used app-custom-table.
- **Hub owns the page chrome (parity with list-role):** the hub renders ONE header row — title left, a single common **Add** button top-right whose label + destination + permission follow the active tab (`Add theme`→`THEME_PRESET.NEW`/themeManagement, `Add branding`→`BRANDING_PRESET.NEW`/brandingManagement, `Add`→`ANNOUNCEMENT.ADD`/announcementManagement) via `AppSettingsHubComponent.currentAdd` + `onAdd()`. Each child list renders ONLY its table — its own `.page-header-section` (title + per-tab Add) is removed from markup, so no duplicate title/button. This fixed the reported card-in-card background mismatch: the hub's parent card is gone; the page uses list-role's flat chrome (`.settings-content-container` padding `1.5rem 2rem`, `.p-tabview` flush, child `.content-card` forced transparent/no-border/no-shadow via `::ng-deep`). Header `<h2>` uses the exact list-role type ramp (`clamp(1.25–1.375rem)`, weight 600, letter-spacing `-0.025em`).
- Buttons: hub Add uses `pButton .modern-button` (same as list-role); list Add/popup actions in the child lists use shared `<app-button>` (variant primary/ghost/danger); per-row action icons stay `.icon-btn` (need per-cell tooltip + disabled). New i18n `THEME_SETTINGS.*` / `BRANDING.*` (LIBRARY, ADD_*, PRESET, PREVIEW, WATERMARK, SEARCH_PLACEHOLDER, DELETE_*, etc.) across all 10 locales.
- Live-verified on :4200 (authed browser): computed styles confirm Add button right-aligned to header edge, h2 19.25px/600/-0.48px, child `.content-card` background `rgba(0,0,0,0)`, child header `REMOVED_FROM_DOM`; tab switch flips Add label (Add theme / Add branding / Add) and fires the right paged endpoint (`/theme/presets`, `/branding/presets`, `/announcements` — all `?page=1&limit=50`). Gates: FE tsc 0, ngc 0, prod build 0; BE tsc 0 + 206 tests. version_261, awaiting push.

### 2026-08-07 — Theme tab: preset library (gallery + try-out)
- Rebuilt `ThemeSettingsComponent` into a **preset gallery** (card grid: swatch strip, Active/Built-in badges, per-card actions) + a collapsible colour editor below. Actions: **Try out** (live preview via ThemeService, unsaved, revertible), **Apply** (persist active via `POST /theme/presets/:id/activate`), **View colours** (load into editor), **Delete** (custom only — seeded/active blocked). Editor Save → active preset (`POST /theme`); "Save as new" dialog → `POST /theme/presets`. The per-section reset buttons are GONE (reset = apply Default Light). `ThemeSettingsService` gained `presets` signal + loadPresets/activate/create/update/delete (dropped `reset`). New `THEME_SETTINGS.*` preset i18n ×10 (RESET/RESET_SECTION removed). Gallery/cards/badges styled with the app's card+icon-btn chrome.
- Verified FE tsc 0, ngc 0, prod build 0 (2.69 MB). BE side verified (206 tests + DB txn sim). Not live-clicked (dev login lockout). version_261.

### 2026-08-06 — Theme tab: full colour-set editor
- Done: Rebuilt `ThemeSettingsComponent` (App Settings hub, first tab, `/app/settings/app`) from the 4-brand-field form into a registry-driven editor over ALL 44 colour tokens (`src/app/shared/theme/theme-tokens.ts`, mirrored with BE). Collapsible `.token-group` sections (brand open by default), each token = `app-custom-input` (hex) + native colour swatch. Live preview via debounced `themeForm.valueChanges` → `ThemeService.applyFromLogin({colors})` on this tab; `ngOnDestroy` restores the authoritative theme. Save sends only non-blank keys as `{colors}` (BE merges). `ThemeService.resolveVars` rewritten to loop the registry + compute derived rgb/alpha companions. Pre-auth theming: `LoginService.fetchAndApplyPublicTheme(org)` hits the new unauthenticated `GET /theme/public?org=` from `login()` + `RelayComponent.ngOnInit` so login/relay paint in the org theme before session. New i18n `THEME_SETTINGS.GROUPS/TOKENS` ×10 locales. `THEME.PUBLIC` api const.
- Verified: `tsc` 0, `ngc` 0, `ng build --configuration production` 0. BE side live-verified on the :3000 dev API. Live FE screenshot of the settings tab NOT captured (token-seed recipe bounced off the settings roleGuard; form-login automation flaky) — structurally validated by ngc/build; to view, log in normally on the :4200 dev FE → Settings → Theme.
- Files: components/theme-settings/*, services/theme-settings.service.ts (unchanged — pass-through), core/services/theme.service.ts, core/services/login.service.ts, modules/auth/components/relay/relay.component.ts, core/constants/api.constant.ts, shared/theme/theme-tokens.ts (new), assets/i18n/*.json.

### 2026-07-24 — Current state captured
- Done: Two tabbed Settings hubs + `SettingsTabForm` shared-Save contract (f93460a1). System Settings hub + SSO config UI + SAML login FE (fe254971, BE 4e7763b/5f68ab7/b66b104). AI Features tab + Dex polish (dd76b9d7, 6ece88d4). Theme/Branding/Email/Security/Announcements tabs. Leaf-permission gating fix for both hubs. Write-only secret masking (SSO cert / email pw / AI key). `backfill-settings` endpoint for existing orgs.
- In progress / Known issues: SSO IdP round-trip unverified; backfill not verified on a live existing org; Dex needs a frontier model. UI commits local-only on `version_261`.
- Next: SSO + backfill live verification.
- Files touched: docs/context/modules/app-settings.md
### 2026-07-24 — Initialized from repo audit
- Done: Context + Goals seeded from the actual code (module folder `src/app/modules/app-settings`).
- In progress: —
- Next: Enrich with specifics as work touches this module.
- Blockers: —
- Files touched: docs/context/modules/app-settings.md
